

export class App {
  constructor(elements): {
    //volume: doc.querySelector('input.volume'),
    // midiSourcepitch: doc.querySelector('input.pitch'),
    // wave: doc.querySelector('select.wave'),
    // note: doc.querySelector('aside#note')
  }
}
